# HuggingFace 토큰 (generate-image 도구용)
# https://hf.co/settings/tokens 에서 발급
#
# HTTP 트랜스포트에서는 클라이언트가 x-hf-token 헤더로 전달할 수 있으며,
# 헤더가 없을 때 이 환경변수가 폴백으로 사용됩니다.
# stdio 트랜스포트에서는 이 환경변수만 사용됩니다.
HF_TOKEN=
